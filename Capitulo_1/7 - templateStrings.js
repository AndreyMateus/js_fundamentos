const nome = 'Rebeca'

// Usando o TEMPLATE STRING/INTERPOLAÇÃO em JS
// OBS: ele também surporta QUEBRAS DE LINHA normais
const concatenacao = `Olá ${nome}`

console.log(concatenacao)

// expressões dentro da interpolação
console.log(`1 + 1 = ${1 + 1}`)
